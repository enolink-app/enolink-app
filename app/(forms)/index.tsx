import { View, Text } from "react-native";

export default function NewEventScreen() {
    return (
        <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
            <Text>Tela de index do forms</Text>
        </View>
    );
}
